name: Run Tests
on:
  push:
    branches: [master]
    tags:
  pull_request:
jobs:
  tox:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.8
      - run: pip install tox
      - run: make test
